#include <stdio.h>
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: takes string
 * Return: the length of the sting
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
